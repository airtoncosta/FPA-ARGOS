/* Malha Fina BPA: somente registros reais, evidência por competência e escopo explícito. */
window.MalhaFinaEngine=(function(){
'use strict';
let result=null,running=false,page=0,filter='';const size=75;
const label=v=>String(v||'').replace(/_/g,' ').toLowerCase().replace(/^./,c=>c.toUpperCase());
const escape=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmtMoney=v=>{const n=typeof v==='number'?v:Number(v);return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'—';};
const fmtTotal=v=>v===null||v===undefined?'Não disponível':fmtMoney(v);
async function json(url,expectedSha256){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status+' ao carregar '+url);const body=await r.text();if(expectedSha256){if(!window.crypto?.subtle)throw Error('SHA-256 indisponível para conferir o snapshot CNES.');const digest=await window.crypto.subtle.digest('SHA-256',new TextEncoder().encode(body));const actual=[...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,'0')).join('');if(actual!==expectedSha256)throw Error('SHA-256 do snapshot CNES diverge do manifesto.');}return JSON.parse(body.replace(/^\uFEFF/,''));}
async function cnesPublishedEntries(){
 const entries={};
 try{
  const auto=await json('cnes_data/auto/manifest.json');
  if(auto?.scope?.municipality_ibge!=='210120'||!auto.competencies||Array.isArray(auto.competencies))return entries;
  for(const [cm,item] of Object.entries(auto.competencies)){
   const relative=String(item?.snapshot?.path||'');
   if(!/^\d{4}(0[1-9]|1[0-2])$/.test(cm)||item?.status!=='published'||!new RegExp('^snapshots/'+cm+'/rev-[0-9]+-[a-f0-9]{64}\\.json$').test(relative))continue;
   if(!/^[a-f0-9]{64}$/.test(String(item.snapshot.sha256||'')))continue;
   const valid=item.coverage?.st===true&&item.coverage?.pf===true&&item.counts?.quarantined===0;
   entries[cm]={
    url:'cnes_data/auto/'+relative,
    fonte:'DATASUS CNES ST/PF (PySUS FTP origin)',
    oficial:true,
    completo:valid,
    cobertura:{profissionais:item.coverage?.pf===true&&item.counts?.quarantined===0,servicos:item.coverage?.services===true,habilitacoes:item.coverage?.habilitations===true},
    sha256:item.snapshot.sha256
   };
  }
 }catch(_error){/* Sem publicação automática; usa somente o manifesto legado. */}
 return entries;
}
let aliasesCache=null;
function normCompAAAAMM(v){const s=String(v||'').trim();let m=s.match(/^(\d{4})[-\/]?(\d{2})$/);if(m&&/^\d{4}(0[1-9]|1[0-2])$/.test(m[1]+m[2]))return m[1]+m[2];m=s.match(/^(0[1-9]|1[0-2])\/(\d{4})$/);if(m)return m[2]+m[1];const d=s.replace(/\D/g,'');if(/^\d{4}(0[1-9]|1[0-2])$/.test(d))return d;return '';}
async function cabecalhosCnesAutorizados(){const headers={};try{const g=typeof window!=='undefined'?window:(typeof globalThis!=='undefined'?globalThis:{});const supa=g.SupabaseConfig;const client=supa&&typeof supa.getClient==='function'?supa.getClient():null;if(client&&client.auth&&typeof client.auth.getSession==='function'){const sessionResult=await client.auth.getSession().catch(()=>null);const token=sessionResult&&sessionResult.data&&sessionResult.data.session&&sessionResult.data.session.access_token;if(token)headers.Authorization='Bearer '+token;}if(!headers.Authorization&&supa&&typeof supa.getAnonKey==='function'){const anon=supa.getAnonKey();if(anon)headers.Authorization='Bearer '+anon;}}catch(_e){}return headers;}
async function carregarBaseCnesViaApi(cm){const r=await fetch('/api/cnes/bacabal?competencia='+cm,{cache:'no-store',headers:await cabecalhosCnesAutorizados()});if(!r.ok)throw Error('HTTP '+r.status+' na API CNES');const data=await r.json();if(String(data.codigoIbge||'')!=='210120')throw Error('Snapshot CNES da API fora do escopo Bacabal.');const declared=normCompAAAAMM(data.competencia||'');if(declared!==cm)throw Error('Competência interna da base diverge da solicitada.');if(!Array.isArray(data.estabelecimentos))throw Error('Resposta da API CNES sem estabelecimentos.');const publicado=data.source_type==='published_snapshot'||data.sourceType==='published_snapshot';const coberturaApi=data.coverage||{};const countsApi=data.counts||{};const completo=publicado?(coberturaApi.st===true&&coberturaApi.pf===true&&countsApi.quarantined===0):true;const cobertura=publicado?{profissionais:coberturaApi.pf===true&&countsApi.quarantined===0,servicos:coberturaApi.services===true,habilitacoes:coberturaApi.habilitacoes===true}:{profissionais:true,servicos:true,habilitacoes:true};return{...data,competencia:declared,fonte:'API CNES autorizada (/api/cnes/bacabal)'+(publicado?' — snapshot publicado':' — base legada'),oficial:true,completo,cobertura};}
function normalizarMapaAliases(data){const mapa=data&&data.municipio_ibge==='210120'&&data.aliases&&typeof data.aliases==='object'?data.aliases:{};const normalizado={};for(const[atual,lista]of Object.entries(mapa)){const principal=String(atual).replace(/\D/g,'');const historicos=[...new Set((Array.isArray(lista)?lista:[lista]).map(x=>String(x??'').replace(/\D/g,'')).filter(x=>/^\d{7}$/.test(x)&&x!==principal))];if(/^\d{7}$/.test(principal)&&historicos.length)normalizado[principal]=historicos;}return normalizado;}
async function carregarAliasesVersionados(){if(aliasesCache)return aliasesCache;aliasesCache={};try{const data=await json('cnes_data/cnes_aliases_210120.json');Object.assign(aliasesCache,normalizarMapaAliases(data));if(Object.keys(aliasesCache).length)return aliasesCache;}catch(_e){}try{const r=await fetch('/api/cnes/aliases?ibge=210120',{cache:'no-store',headers:await cabecalhosCnesAutorizados()});if(!r.ok)throw Error('HTTP '+r.status+' na API de aliases');Object.assign(aliasesCache,normalizarMapaAliases(await r.json()));}catch(_e2){}return aliasesCache;}
function fontesVivasCnes(){const fontes=[];try{const g=typeof window!=='undefined'?window:(typeof globalThis!=='undefined'?globalThis:{});const estado=g.CnesModule?.state;if(estado&&Array.isArray(estado.estabelecimentos)&&estado.estabelecimentos.length)fontes.push({estabelecimentos:estado.estabelecimentos,competencia:normCompAAAAMM(estado.competenciaAtiva||estado.competencia||''),origem:'Menu CNES & Vínculos (vínculos vigentes em tempo de execução)'});const argos=g.ArgosCnesBase;if(argos&&Array.isArray(argos.estabelecimentos)&&argos.estabelecimentos.length)fontes.push({estabelecimentos:argos.estabelecimentos,competencia:normCompAAAAMM(argos.competencia||''),origem:'Cache CNES vigente (tempo de execução)'});const ppc=g.ProducaoProfissionalModule?.cnesCache;if(ppc&&Array.isArray(ppc.estabelecimentos)&&ppc.estabelecimentos.length)fontes.push({estabelecimentos:ppc.estabelecimentos,competencia:normCompAAAAMM(ppc.competencia||''),origem:'Cache de produção (tempo de execução)'});}catch(_e2){}return fontes;}
async function enriquecerBaseCnes(cm,baseCnes,sources){
 const dig=v=>String(v??'').replace(/\D/g,'');const unidades=Array.isArray(baseCnes.estabelecimentos)?baseCnes.estabelecimentos:[];const porCodigo=new Map();
 for(const u of unidades){if(dig(u.cnes))porCodigo.set(dig(u.cnes),u);}
 const aliases=await carregarAliasesVersionados();let aliasesAplicados=0;
 for(const[principal,historicos]of Object.entries(aliases)){const alvo=porCodigo.get(principal);if(!alvo)continue;alvo.aliases=[...new Set([...(Array.isArray(alvo.aliases)?alvo.aliases.map(String):[]),...historicos])];for(const h of historicos)if(!porCodigo.has(h))porCodigo.set(h,alvo);aliasesAplicados++;}
 if(aliasesAplicados>0)sources.push({tipo:'CNES aliases',competencia:cm,estado:'Aplicados',fonte:'cnes_data/cnes_aliases_210120.json (códigos históricos versionados)'});
 const vivas=fontesVivasCnes().filter(f=>f.competencia&&f.competencia===cm);let vinculosAdicionados=0;
 for(const fonte of vivas){for(const u of(fonte.estabelecimentos||[])){const alvo=porCodigo.get(dig(u.cnes));if(!alvo||!Array.isArray(u.profissionais))continue;alvo.profissionais=Array.isArray(alvo.profissionais)?alvo.profissionais:[];const conhecidos=new Set(alvo.profissionais.map(p=>dig(p.cns||p.cnsMaster)));for(const p of u.profissionais){const cns=dig(p.cns||p.cnsMaster);if(cns&&!conhecidos.has(cns)){conhecidos.add(cns);alvo.profissionais.push({cns:p.cns||p.cnsMaster,cnsMaster:p.cnsMaster||p.cns,cbo:p.cbo,nome:p.nome,fonteViva:true});vinculosAdicionados++;}}}}
 if(vinculosAdicionados>0)sources.push({tipo:'CNES vínculos vigentes',competencia:cm,estado:vinculosAdicionados+' vínculo(s) incorporado(s)',fonte:vivas.map(v=>v.origem).filter((v,i,a)=>a.indexOf(v)===i).join('; ')});
}
async function loadBases(competencias){
 let manifest,sources=[];try{manifest=await json('audit_data/manifest.json');}catch(e){manifest={cnes:{},sigtap:{}};sources.push({tipo:'Manifesto legado',competencia:'',estado:'Indisponível',fonte:e.message});}
 const legadoCnes={...(manifest.cnes||{})};
 manifest.cnes={...legadoCnes,...await cnesPublishedEntries()};
 const bases={};
 await Promise.all(competencias.filter(Boolean).map(async cm=>{bases[cm]={};
 const candidatos=[];if(manifest.cnes?.[cm])candidatos.push({origem:'static',item:manifest.cnes[cm]});candidatos.push({origem:'api'});if(legadoCnes[cm]&&legadoCnes[cm].url!==manifest.cnes?.[cm]?.url)candidatos.push({origem:'static',item:legadoCnes[cm]});
 for(const cand of candidatos){try{
 if(cand.origem==='api'){bases[cm].cnes=await carregarBaseCnesViaApi(cm);}else{const data=await json(cand.item.url,cand.item.sha256);const declared=data.competencia||data.versao?.replace(/\D/g,'');if(declared!==cm)throw Error('Competência interna da base diverge da solicitada.');if(cand.item.url.startsWith('cnes_data/auto/')&&String(data.codigoIbge)!=='210120')throw Error('Snapshot CNES automático fora do escopo Bacabal.');bases[cm].cnes={...data,competencia:declared,fonte:cand.item.fonte,oficial:cand.item.oficial===true,completo:cand.item.completo===true,cobertura:cand.item.cobertura||{}};}
 sources.push({tipo:'CNES',competencia:cm,estado:bases[cm].cnes.oficial?'Carregado':'Origem não validada',fonte:bases[cm].cnes.fonte});break;
 }catch(e){sources.push({tipo:'CNES',competencia:cm,estado:'Indisponível',fonte:(cand.origem==='api'?'API CNES autorizada':(cand.item.url||'base local'))+': '+e.message});}}
 const itemSigtap=manifest.sigtap?.[cm];if(!itemSigtap){sources.push({tipo:'SIGTAP local',competencia:cm,estado:'Sem base local',fonte:'Base desta competência ausente'});}else{try{const data=await json(itemSigtap.url,itemSigtap.sha256);const declared=data.competencia||data.versao?.replace(/\D/g,'');if(declared!==cm)throw Error('Competência interna da base diverge da solicitada.');bases[cm].sigtap={...data,competencia:declared,fonte:itemSigtap.fonte,oficial:itemSigtap.oficial===true,completo:itemSigtap.completo===true,cobertura:itemSigtap.cobertura||{}};sources.push({tipo:'SIGTAP',competencia:cm,estado:itemSigtap.oficial?'Carregado':'Origem não validada',fonte:itemSigtap.fonte||itemSigtap.url});}catch(e){sources.push({tipo:'SIGTAP',competencia:cm,estado:'Indisponível',fonte:e.message});}}
 if(bases[cm].cnes){await enriquecerBaseCnes(cm,bases[cm].cnes,sources);}}));return {bases,sources};
}
function modal(){let m=document.getElementById('modalMalhaFinaResultados');if(!m){m=document.createElement('div');m.id='modalMalhaFinaResultados';m.className='mf-modal-results-overlay hidden';m.setAttribute('role','dialog');m.setAttribute('aria-modal','true');m.setAttribute('aria-label','Diagnóstico da produção BPA');m.innerHTML='<div class="mf-modal-results-wrapper"><button type="button" class="mf-btn-close" onclick="MalhaFinaEngine.fecharModalResultados()" aria-label="Fechar diagnóstico">×</button><div id="malhaFinaResultadosContent" aria-live="polite"></div></div>';document.body.appendChild(m);}m.classList.remove('hidden');return document.getElementById('malhaFinaResultadosContent');}
async function executar(dados){
 if(running)return;const selected=dados||window.BpaModule?.filePendingUpload;const text=selected?.conteudo??selected?.conteudo_arquivo;
 if(typeof text!=='string'||!text.trim()){alert('Selecione um arquivo BPA exportado para auditar. Não serão usados dados de exemplo ou de outra tela.');return;}
 running=true;result=null;page=0;filter='';const box=modal();box.innerHTML='<div class="mf-audit-summary"><h2>Lendo o arquivo BPA...</h2><p>Consultando bases da competência de cada registro.</p></div>';
 try{const parsed=window.BpaAuditCore.parse(text);const comps=[...new Set(parsed.records.map(r=>window.BpaAuditCore.competencia(r.competencia)))];const {bases,sources}=await loadBases(comps);
 const apiRecords=parsed.records.filter(r=>!bases[r.competencia]?.sigtap);
 if(apiRecords.length&&window.SigtapAuditApi){box.innerHTML='<div class="mf-audit-summary"><h2>Consultando SIGTAP por competência...</h2><p>Buscando todas as páginas de CBO, CID, serviços, habilitações e regras dos procedimentos presentes no arquivo.</p></div>';const api=await window.SigtapAuditApi.load(apiRecords);for(const [cm,sigtap]of Object.entries(api.bases)){bases[cm]||={};bases[cm].sigtap=sigtap;}sources.push(...api.sources);}
 const computed=window.BpaAuditCore.audit(parsed,bases);computed.arquivo=selected.nomeArquivo||selected.nome_arquivo||'BPA';computed.sources=sources;computed.fingerprint=await fingerprint(text);
 result=computed;render();
 }catch(e){box.innerHTML='<div class="mf-audit-summary mf-inconclusivo"><h2>Auditoria não concluída</h2><p>'+escape(e.message)+'</p><p>Não foi emitido parecer de conformidade.</p></div>';}
 finally{running=false;}
 return result;
}
async function fingerprint(text){if(!window.crypto?.subtle)return 'Indisponível';const bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(text));return [...new Uint8Array(bytes)].map(b=>b.toString(16).padStart(2,'0')).join('');}
function render(){if(!result)return;const r=result;const box=modal();const title=r.status==='CONFORME'?'Tudo OK nas verificações concluídas':r.status==='NAO_CONFORME'?'Foram encontradas não conformidades':'Auditoria inconclusiva';
 const visible=r.findings.filter(f=>!filter||f.status===filter);const pages=Math.max(1,Math.ceil(visible.length/size));page=Math.min(page,pages-1);
 box.innerHTML='<div class="mf-audit-summary mf-'+r.status.toLowerCase()+'"><span class="mf-badge">AUDITORIA DA PRODUÇÃO BPA</span><h2>'+title+'</h2><p><strong>'+escape(r.arquivo)+'</strong> • '+r.totalLinhas+' registros reais • Competências de atendimento: '+escape(r.competencias.join(', '))+'</p><p>'+(r.status==='CONFORME'?'Nenhuma não conformidade nas regras aplicáveis implementadas e bases consultadas.':'Confira abaixo os erros, alertas e verificações que não puderam ser concluídas.')+' Este diagnóstico prévio não substitui o processamento oficial do SIA/SUS.</p><p class="mf-financial-note"><strong>Valor de referência SUS (SA): '+fmtTotal(r.registrosValorizados? r.valorReferenciaSa:null)+'</strong> • '+(r.registrosValorizados||0)+' registros valorizados. Esse valor é uma referência da tabela SIGTAP; não representa pagamento confirmado nem calcula glosa automaticamente.</p></div>'+
 '<div class="mf-kpis-grid"><div class="mf-kpi-card"><div><span class="kpi-lbl">Não conformidades</span><strong class="kpi-val">'+r.naoConformidades+'</strong></div></div><div class="mf-kpi-card"><div><span class="kpi-lbl">Não verificados</span><strong class="kpi-val">'+r.naoVerificados+'</strong></div></div><div class="mf-kpi-card"><div><span class="kpi-lbl">Alertas</span><strong class="kpi-val">'+r.alertas+'</strong></div></div><div class="mf-kpi-card"><div><span class="kpi-lbl">Registros conformes</span><strong class="kpi-val">'+r.registrosConformes+' / '+r.totalLinhas+'</strong></div></div><div class="mf-kpi-card mf-kpi-financial"><div><span class="kpi-lbl">Valor de referência (SA)</span><strong class="kpi-val">'+fmtTotal(r.registrosValorizados? r.valorReferenciaSa:null)+'</strong><small>Somente registros com valor SIGTAP</small></div></div><div class="mf-kpi-card mf-kpi-financial"><div><span class="kpi-lbl">Valor em linhas apontadas</span><strong class="kpi-val">'+fmtTotal(r.registrosValorizados? r.valorNaoConformeSa:null)+'</strong><small>Referência SA; não é glosa calculada</small></div></div><div class="mf-kpi-card mf-kpi-financial"><div><span class="kpi-lbl">Sem valor SIGTAP</span><strong class="kpi-val">'+(r.registrosSemValor||0)+'</strong><small>Requer base da competência</small></div></div></div>'+
 '<details class="mf-audit-bases" open><summary>Bases e cobertura das verificações</summary><ul>'+r.sources.map(s=>'<li><strong>'+escape(s.tipo+' '+s.competencia)+'</strong>: '+escape(s.estado)+' — '+escape(s.fonte)+'</li>').join('')+'</ul><p>As bases de outro mês não comprovam o vínculo no mês do atendimento. No BPA-C, CNS individual, CID e sexo não constam no layout.</p><div class="mf-table-wrapper"><table class="mf-table"><thead><tr><th>Regra</th><th>Conformes</th><th>Não conformes</th><th>Não verificados</th><th>Não aplicável</th></tr></thead><tbody>'+Object.entries(r.checks).map(([name,c])=>'<tr><td>'+escape(label(name))+'</td><td>'+c.CONFORME+'</td><td>'+c.NAO_CONFORME+'</td><td>'+c.NAO_VERIFICADO+'</td><td>'+c.NAO_APLICAVEL+'</td></tr>').join('')+'</tbody></table></div></details>'+
 '<div class="mf-table-section"><div class="mf-table-header"><h3>Diagnóstico por linha</h3><select aria-label="Filtrar diagnóstico" onchange="MalhaFinaEngine.filtrar(this.value)">'+[['','Todos'],['NAO_CONFORME','Não conformidades'],['NAO_VERIFICADO','Não verificados'],['ALERTA','Alertas']].map(([value,label])=>'<option value="'+value+'" '+(filter===value?'selected':'')+'>'+label+'</option>').join('')+'</select><button type="button" class="btn-mf-export-excel" onclick="MalhaFinaEngine.exportarExcel()">Exportar diagnóstico</button></div><div class="mf-table-wrapper"><table class="mf-table"><thead><tr><th>Linha / competência</th><th>Resultado / regra</th><th>CNES / CNS profissional</th><th>Procedimento</th><th>Valor SA</th><th>Diagnóstico</th><th>Esperado / encontrado</th></tr></thead><tbody>'+visible.slice(page*size,(page+1)*size).map(f=>'<tr><td>'+f.linha+'<br>'+escape(f.competencia)+'</td><td>'+escape(label(f.status))+'<br>'+escape(label(f.regra))+'</td><td>'+escape(f.cnes)+'<br>'+escape(f.cns)+'</td><td>'+escape(f.procedimento)+'</td><td class="mf-value-cell">'+fmtMoney(f.valorSa)+'</td><td>'+escape(f.mensagem)+'</td><td>'+escape(f.esperado)+'<br>'+escape(f.encontrado)+'</td></tr>').join('')+(visible.length?'':'<tr><td colspan="7">Nenhum apontamento neste filtro.</td></tr>')+'</tbody></table></div><div class="mf-audit-pagination"><button type="button" onclick="MalhaFinaEngine.pagina(-1)" '+(page===0?'disabled':'')+'>Anterior</button><span>Página '+(page+1)+' de '+pages+' • '+visible.length+' apontamentos</span><button type="button" onclick="MalhaFinaEngine.pagina(1)" '+(page+1===pages?'disabled':'')+'>Próxima</button></div></div><p class="mf-audit-footnote">Emitido em '+escape(new Date(r.geradoEm).toLocaleString('pt-BR'))+' • SHA-256 do conteúdo interpretado: '+escape(r.fingerprint)+'</p>';
}
function exportarExcel(){if(!result)return;const data=result.findings.map(f=>Object.fromEntries(Object.entries(f).map(([k,v])=>[k,typeof v==='string'&&/^[=+@-]/.test(v)?"'"+v:v])));if(window.XLSX){const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(data),'Diagnostico');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(result.sources),'Bases');XLSX.writeFile(wb,'Diagnostico_BPA_'+Date.now()+'.xlsx');}else{const blob=new Blob([JSON.stringify(result,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Diagnostico_BPA.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}}
 const engine = {executar,exportarExcel,getUltimoResultado:()=>result,fecharModalResultados:()=>document.getElementById('modalMalhaFinaResultados')?.classList.add('hidden'),pagina:delta=>{page+=delta;render();},filtrar:value=>{filter=value;page=0;render();},loadBases};
 if(typeof module==='object'&&module.exports) module.exports = engine;
 return (typeof window !== 'undefined' && window.PenteFinoEngine) ? window.PenteFinoEngine : engine;
})();
